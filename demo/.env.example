# 둘 다 채우면 데모에서 구글 시트 동기화 UI가 켜진다 (없으면 picker+override만).
# 로컬은 demo/.env.local 에 복사해 넣고, Vercel은 Project Settings > Environment Variables 에 등록.
VITE_SHEETS_CLIENT_ID=your-client-id.apps.googleusercontent.com
VITE_SHEETS_SPREADSHEET_ID=your-spreadsheet-id
